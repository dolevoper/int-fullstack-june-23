const userChosenProduct = prompt(
	'Please choose a product to buy:\n 1. red\n green\n blue'
)

// checkUserChosenProduct()

// function checkUserChosenProduct() {
// 	if (userChosenProduct === ) {
// 		const userConfirmation = prompt(
// 			'You have been charged' + red.price + '. Please confirm [y/yes] [n/no]'
// 		)
// 		if (userConfirmation === 'y' || 'yes') {
// 			alert('Confirmation succesfull')
// 		} else if (userConfirmation === 'n' || 'no') {
// 			alert('Confirmation cancelled')
// 		}
// 	}
// }

const red = {
	price: 3.99,
	Amount: 20,
	aisle: 2,
}

const green = {
	price: 5.99,
	Amount: 40,
	aisle: 4,
}

const blue = {
	price: 7.99,
	Amount: 60,
	aisle: 6,
}
