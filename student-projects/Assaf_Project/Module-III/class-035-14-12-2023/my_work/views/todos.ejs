<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todos</title>
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <header class="--padding">
        <h1>Todos</h1>
    </header>
    <main class="--padding">
        <form action="/addTodo" method="post">
            <input type="text" name="todo" autofocus />
            <button type="submit">Add</button>
        </form>
        <ul>
            <% for (let todo of todos) { %>
                <li>
                    <span class="--inline-flex">
                        <%= todo %>
                            <form action="/toggleTodo" method="post">
                                <button class="toggleBtn" type="submit">Toggle</button>
                            </form>
                    </span>
                </li>
                <% } %>
        </ul>
        <p class="--padding">Todos count: <%= todos.length %>
        </p>
        <form action="/resetTodos" method="post">
            <button type="submit">Reset todos</button>
        </form>
    </main>
    <article class="--padding">
        <p>
            <%= visits %> "people" has viewed this page
        </p>
    </article>
</body>

</html>