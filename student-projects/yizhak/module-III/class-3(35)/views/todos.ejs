<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todos</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
        html {
            font-size: calc(1rem + 0.5vw);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    </style>
</head>

<body>
    <h1>Todos</h1>
    <form action="/addTodo" method="post">
        <input type="text" name="todo" autofocus />
        <button type="submit">Add</button>
    </form>

    <ul>
        <% for (let todo of todos) { %>
            <li>
                <%= todo %> <form action="/toggle" method="post"><button type="submit">toggle</button></form>
            </li>
            <% } %>
    </ul>
    <p>Todos count: <%= todos.length %>
    </p>
    <form action="/resetTodo" method="post">
    <button type="submit">reset todos</button>
    </form>
    <p>
        counter: <%= counter %> 
    </p>
</body>

</html>